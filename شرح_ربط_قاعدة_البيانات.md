# شرح ربط البرنامج بقاعدة البيانات (PostgreSQL)

البرنامج يحتاج قاعدة بيانات اسمها **appointment** ومستخدم اسمه **appointments_user** حتى يحفظ التسجيل والبيانات. اتبع الخطوات بالترتيب.

---

## الخطوة 1: تشغيل PostgreSQL

- إذا عندك **pgAdmin** مثبت، عادةً معه PostgreSQL.
- من قائمة ابدأ (Start) ابحث عن **Services** وافتحها، أو اكتب في Run: `services.msc`
- في القائمة ابحث عن خدمة فيها كلمة **postgres**
- تأكد أن **Status** = Running (يعمل). إذا لا، اضغط يمين → Start

أو ببساطة: **افتح pgAdmin**. إذا فتح بدون خطأ، معناها PostgreSQL شغال.

---

## الخطوة 2: فتح pgAdmin وإنشاء قاعدة البيانات

1. افتح **pgAdmin 4**
2. من الشجرة على اليسار: اضغط على السهم بجانب **Servers**
3. اضغط على **PostgreSQL** (أو الاسم اللي عندك للخادم) — قد يطلب منك كلمة مرور الـ postgres، أدخلها إذا طلبت
4. تحت الخادم ستجد **Databases**
5. **كليك يمين** على **Databases** → اختر **Create** → **Database...**
6. في النافذة:
   - في خانة **Database** اكتب بالضبط: **appointment**
   - باقي الخانات اتركها كما هي
7. اضغط **Save** (أو حفظ)

بهذا صار عندك قاعدة بيانات اسمها **appointment**.

---

## الخطوة 3: إنشاء المستخدم (appointments_user)

1. في pgAdmin من الشجرة على اليسار، تحت نفس الخادم ابحث عن **Login/Group Roles**
2. **كليك يمين** على **Login/Group Roles** → **Create** → **Login/Group Role...**
3. في تبويب **General**:
   - **Name** اكتب: **appointments_user**
4. في تبويب **Definition**:
   - **Password** اكتب: **123456**
   - (أو أي كلمة مرور تريدها، لكن لازم نفس القيمة في الملف application.properties)
5. في تبويب **Privileges**:
   - فعّل (ضع صح) عند **Can login**
6. اضغط **Save**

---

## الخطوة 3.5 (اختياري): إنشاء كل الجداول دفعة واحدة

إذا حاب تنشئ **كل جداول النظام** (مستخدمون، عيادات، أطباء، غرف، مواعيد، مهام، قائمة انتظار، سجل تدقيق، إعدادات) دفعة واحدة:

1. في pgAdmin من الشجرة: كليك يمين على قاعدة **appointment** → **Query Tool**
2. من القائمة: **File** → **Open** (أو من المشروع افتح الملف `AppointmentSystem/database/full_schema_postgresql.sql`)
3. انسخ محتوى الملف كاملًا والصقه في Query Tool
4. اضغط **Execute (F5)**
5. بعدها نفّذ ملف الصلاحيات: افتح `AppointmentSystem/database/grant_permissions.sql` ونفّذه في نفس Query Tool (بعد اختيار قاعدة appointment)

بهذا تصير كل الجداول جاهزة. البرنامج عند أول اتصال ينشئ جدول `app_user` فقط إن لم يكن موجوداً؛ إن نفذت الـ schema الكامل فكل شيء جاهز.

---

## الخطوة 4: إعطاء المستخدم صلاحية على قاعدة appointment

1. من الشجرة اضغط على قاعدة البيانات **appointment** (تحت Databases)
2. من الأعلى افتح **Tools** → **Query Tool** (أو يمين على appointment → Query Tool)
3. في نافذة الكتابة الصق هذا بالكامل ثم اضغط زر التشغيل (Execute / F5):

```sql
-- صلاحية الاتصال بقاعدة appointment
GRANT CONNECT ON DATABASE appointment TO appointments_user;

-- إنشاء واستخدام السكيمة appointment داخل القاعدة
\c appointment
CREATE SCHEMA IF NOT EXISTS appointment;
GRANT USAGE ON SCHEMA appointment TO appointments_user;
GRANT CREATE ON SCHEMA appointment TO appointments_user;
```

**ملاحظة:** إذا Query Tool أظهر خطأ على سطر `\c appointment`، اعمل التالي:
- من القائمة في Query Tool اختر من قائمة قاعدة البيانات (في الأعلى) اسم **appointment** حتى تكون الاستعلامات تنفذ على هذه القاعدة
- ثم نفّذ فقط هذه الأسطر (بدون \c):

```sql
CREATE SCHEMA IF NOT EXISTS appointment;
GRANT USAGE ON SCHEMA appointment TO appointments_user;
GRANT CREATE ON SCHEMA appointment TO appointments_user;
```

4. تأكد أن التنفيذ تم بدون أخطاء حمراء.

---

## الخطوة 5: التأكد من إعدادات البرنامج

في مشروعك، الملف:

**`src/main/resources/application.properties`**

يجب أن يكون فيه هالإعدادات (وتطابق ما عملته في pgAdmin):

- **اسم قاعدة البيانات في الرابط:** `appointment`
- **اسم المستخدم:** `appointments_user`
- **كلمة المرور:** نفس اللي حطيتها للمستخدم (مثلاً `123456`)

مثال الإعدادات:

```properties
database.enabled=true
database.url=jdbc:postgresql://localhost:5432/appointment?currentSchema=appointment
database.username=appointments_user
database.password=123456
```

- إذا PostgreSQL عندك على بورت غير 5432 (مثلاً 5433)، غيّر الرقم في الرابط:  
  `jdbc:postgresql://localhost:5433/appointment?currentSchema=appointment`

---

## الخطوة 6: تشغيل البرنامج والتحقق

1. احفظ كل الملفات، ثم شغّل البرنامج من جديد
2. إذا **ما ظهرت** نافذة "Database not connected" — معناها الاتصال نجح
3. سجّل مستخدم جديد من واجهة البرنامج
4. في pgAdmin:
   - افتح: **Databases** → **appointment** → **Schemas** → **appointment** → **Tables**
   - يجب أن ترى جدول **app_user**
   - كليك يمين على **app_user** → **View/Edit Data** → **All Rows**
   - أو من Query Tool (وأنت متصل بقاعدة appointment) نفّذ:

```sql
SELECT * FROM appointment.app_user;
```

هنا تشوف كل المستخدمين المسجّلين.

---

## لو حاب تستخدم البرنامج بدون قاعدة بيانات (مؤقتاً)

إذا تريد البرنامج يشتغل بدون إعداد PostgreSQL الآن (البيانات تكون في الذاكرة فقط وتضيع عند الإغلاق):

في **application.properties** غيّر السطر إلى:

```properties
database.enabled=false
```

ثم أعد تشغيل البرنامج. لن تظهر رسالة "Database not connected"، لكن البيانات لن تُحفظ في pgAdmin.

---

## ملخص سريع

| المطلوب            | القيمة              |
|--------------------|---------------------|
| اسم قاعدة البيانات | **appointment**     |
| اسم المستخدم       | **appointments_user** |
| كلمة المرور        | **123456** (أو اللي تحطه في pgAdmin) |
| البورت (عادة)      | **5432**            |

---

## فهم رسالة الخطأ في النافذة

بعد التعديل، النافذة "Database not connected" تعرض **تفاصيل الخطأ** تحت النص. استخدمها لمعرفة السبب:

| ما يظهر في "تفاصيل الخطأ" | المعنى | الحل |
|---------------------------|--------|------|
| **Connection refused** أو **Connection refused: connect** | PostgreSQL غير شغال أو البورت غلط | شغّل خدمة PostgreSQL أو غيّر البورت في application.properties إلى 5433 إذا عندك أكثر من نسخة |
| **FATAL: database "appointment" does not exist** | قاعدة البيانات appointment غير موجودة | أنشئ قاعدة البيانات باسم **appointment** من pgAdmin (الخطوة 2 أعلاه) |
| **FATAL: password authentication failed** أو **no pg_hba.conf** | اسم المستخدم أو كلمة المرور غلط | تأكد أن المستخدم **appointments_user** موجود وكلمة المرور في application.properties نفس اللي في pgAdmin |
| **role "appointments_user" does not exist** | المستخدم غير موجود | أنشئ المستخدم من pgAdmin (الخطوة 3 أعلاه) |

بعد ما تشغّل البرنامج وتظهر النافذة، انسخ سطر **"تفاصيل الخطأ"** وأرسله إذا حاب نحدد لك الخطوة بالضبط.

---

إذا واجهت خطأ في أي خطوة، اكتب لي رقم الخطوة ونسخ رسالة الخطأ أو صورة الشاشة لأشرحلك إياها.
